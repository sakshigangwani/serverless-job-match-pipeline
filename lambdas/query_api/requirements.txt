# boto3 ships with the Lambda runtime. common.dynamodb (and its common.models
# dependency) needs pydantic — bundled via the separate PydanticLayer, see
# infra/jobpulse_infra/jobpulse_stack.py.
pydantic==2.13.5
